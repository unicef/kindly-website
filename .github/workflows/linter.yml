name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: yarn
    - name: Run ESLint
      run: yarn run eslint . --fix --ext .js,.jsx,.ts,.tsx
    - name: Run prettier
      run: yarn run prettier --write "./src/**/*.{md,css,json}"